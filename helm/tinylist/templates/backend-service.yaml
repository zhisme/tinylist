apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinylist.backend.name" . }}
  labels:
    {{- include "tinylist.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinylist.backend.selectorLabels" . | nindent 4 }}
