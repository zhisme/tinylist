# Frontend environment variables
# Copy to .env.local for local development overrides

# API URL - defaults to /api/private (relative path for K8s ingress)
# Examples:
#   VITE_API_URL=/api/private          # K8s with ingress routing (default)
#   VITE_API_URL=http://localhost:8080/api/private  # Direct backend access
#   VITE_API_URL=https://api.example.com/api/private  # External API
# VITE_API_URL=
